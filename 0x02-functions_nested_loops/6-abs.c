#include "main.h"
/**
 * _abs - prints the absolute value of an integer
 *
 * Return: void
 */
int _abs(int)
{
	int a;
	int abs;

	if (a < 0)
	{
		abs = -a;
	}
	else
	{
		abs = a;
	}
	return (abs);
}

